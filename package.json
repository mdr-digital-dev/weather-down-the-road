{
  "name": "weather-down-the-road",
  "version": "2.0.0",
  "description": "Route weather planner: shows the forecast at every point along a drive, at the moment you'll actually be there.",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  },
  "license": "MIT"
}
